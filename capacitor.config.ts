import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gameoffortunes.app',
  appName: 'Game of Fortunes',
  webDir: 'dist/gofv2/browser'
};

export default config;
